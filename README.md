# Finance Lab Website

Static website files prepared for GitHub Pages.

Entry point: `index.html`

This package includes the current public pages, shared assets, photo archive images, publication PDFs, course/seminar files used by the top-level pages, and the full legacy `Students/` archive. `students.html` also loads the current list from Google Sheets when available.
