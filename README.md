# Finance Lab Website

Static website files prepared for GitHub Pages.

Entry point: `index.html`

This lite package includes the current public pages, shared assets, photo archive images, publication PDFs, and course/seminar files used by the top-level pages. The full legacy `Students/` archive is intentionally not bundled because it is too large for a practical GitHub Pages repository; `students.html` loads the current list from Google Sheets.
